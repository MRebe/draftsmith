---
description: Run the Draftsmith revision pass over prose - a file, a path, or pasted text.
---

Invoke the `draftsmith` skill, then run its revision pass over the target below.

Target: $ARGUMENTS

If no target is given, ask which text to revise before doing anything else.

Apply the three steps in order - deletion test, substitution test, frequency check - sentence by
sentence. Then report what changed and why, grouped by the habit that triggered each edit. Do
not rewrite anything that passes all three tests.
